# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets — keep the template tracked, ignore everything else
.env
.env.*
!.env.example

# SQLite (local DB files)
# health.db ships seed data (profiles/test_items/measurements) to Render —
# the app reads it through get_local_conn(), so it must be in git.
data/*.db
!data/health.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# OS
.DS_Store

# Claude Code scratch workspace
.claude/
